<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 shadow-sm">
  <div class="container">
    <%= link_to "💰 Controle de Despesas", root_path, class: "navbar-brand fw-bold" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "📊 Dashboard", dashboard_path, class: "nav-link text-white" %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to "📋 Despesas", expenses_path, class: "nav-link text-white" %>
        </li>
      </ul>

      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "⚙️ Configurações", "#configModal", class: "nav-link text-white", data: { bs_toggle: "modal" } %>
          </li>
          <li class="nav-item">
            <%= link_to "👤 Perfil", edit_user_registration_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
            <%= button_to "🚪 Sair", destroy_user_session_path, method: :delete,
                            class: "nav-link btn btn-link text-white",
                            data: { turbo_confirm: "Deseja realmente sair?" } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "🔑 Entrar", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "📝 Cadastrar", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>